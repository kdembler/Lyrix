import { useCallback } from 'react'

export const useClipboard = () => {
  // const writeToClipboard = useCallback((value: string) => {
  //   if (window.Main) {
  //     window.Main.sendMessage(value)
  //   } else {
  //     console.error('Failed to communicate with Electron')
  //   }
  // }, [])
  //
  // return { writeToClipboard }
}
